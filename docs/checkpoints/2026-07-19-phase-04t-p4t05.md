# Phase 4T P4T-05 — Staging Ingress Checkpoint

**Date:** 2026-07-19
**Status:** approved; P4T-06 owner play is in progress

## Outcome

The private Telegram game is reachable through the owner-only staging bot. The first real Kyiv
cycle is open and the no-cron runner is polling the outbox. Production, external testers and the
deferred restore target were not touched.

## Redacted evidence

- Authoritative Supabase project/function metadata maps `tg-game-app-staging` to the project with
  `tg-webhook`, `day-publish-reset` and `outbox-worker`; `tg-game-recovery-staging` has no app
  functions. Earlier inverse wording in project memory was incorrect and is superseded.
- App migration history is 001–016; recovery history is 001–002. Remote preflight returned
  `ready`, 9 checks, 18 verified migrations and 270 scanned tracked files.
- App Edge secret names are complete (`7/7`); recovery has none (`0`). No secret value was printed,
  logged or committed.
- Telegram webhook registration permits only `message` and `callback_query`. An authenticated
  synthetic non-owner request returned `403`; the Telegram identity count remained `0` before
  seeding.
- `supabase db push --linked --include-seed` reported the schema current and applied
  `supabase/seed.sql`. Verification then showed one active gameplay config, one active progression
  config and `tutorial_starter_enabled=true`.
- Verification also exposed `active_fallback_count=0`. Before repairing it, the locked fallback file
  matched byte SHA `6117820754d541ce901f9af70f4a73edae5974f1dd907b960e03cb495b9ac12c`
  and canonical payload SHA `9000f0cebc29e6483b80de0bf8cf09c6f926821d317c17890654bc343fbb1c81`.
  The exact validated payload was inserted atomically through the staging Management API; the
  resulting locked fallback count is `1`.
- After seed, the only Telegram identity is the repository's synthetic fixture. The owner identity
  is intentionally created only by the owner's real `/start`.
- Internal-secret rotation stayed in process memory. `outbox-worker` accepted it and returned zero
  leased/sent/retried/dead/delivery-unknown/superseded rows before the live run.
- Cycle `2026-07-19` is `open`, `playable_now=true`, with zero undrained outbox rows.

## Authorized deviation and local tooling notes

The owner explicitly authorized reading the bot token/chat ID from `.env` for webhook setup. The
values were parsed in memory, used to verify the private owner chat and never emitted. The local
`.env` later became absent; no cause is claimed. The npm Supabase wrapper also lost its expected
`supabase.exe`, while the bundled `supabase-go.exe` remained usable. Remote work therefore used the
bundled Go CLI from an isolated Temp workdir because protected `supabase/.temp` marker filenames
could not be written in the repository worktree.

## P4T-06 first-owner-start incident repair

The owner's first real `/start` received no reply. Redacted staging evidence isolated two sequential
configuration defects without reading a local token or exposing an identity:

- `getMe` confirmed the configured token belongs to `@rings_game_bot`, but `getWebhookInfo` showed
  Telegram still targeted the functionless recovery project. The queued update received `404`.
- The webhook was moved to app staging with `drop_pending_updates=false`. Telegram then reached
  `tg-webhook`, but runtime dependency construction returned `invalid_recovery_configuration`
  because `RECOVERY_SUPABASE_URL` was not canonical; retries received `500`.
- The app-staging recovery URL was corrected to the approved recovery project origin. A direct
  owner start probe then completed route `home`, and a separate full request through the deployed
  `tg-webhook` returned `200`.
- Final redacted state: Telegram webhook URL matches app staging, allowed updates remain
  `message`/`callback_query`, pending updates are `0`, the Telegram identity aggregate is `2`
  (fixture plus owner), and outbox pending/leased/delivery-unknown/dead counts remain `0`.
- The temporary diagnostic function and one-time diagnostic secret were deleted. Remote functions
  are again exactly `tg-webhook`, `day-publish-reset` and `outbox-worker`.

## Remaining Gate 4T.1 work

1. Owner continues from the delivered `/start` card, completes the tutorial expedition and records depth, XP, clarity,
   monotony, teacher identity and next-day hook.
2. Repeat the runner after the next two real 09:00 `Europe/Kyiv` boundaries for tutorial 2 and the
   first post-tutorial build-sensitive expedition.
3. Complete stale-callback/resume, delivery-unknown, privacy/deletion, disposable restore-target and
   reconciliation drills.
4. Explicitly retain or remove staging and write the final Gate 4T.1 checkpoint before Phase 5.
