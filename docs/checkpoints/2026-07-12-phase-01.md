# Phase 1 checkpoint — 2026-07-12

## Verdict

**Status: APPROVED / local deterministic kernel complete.**

Phase 1 реалізовано й перевірено в isolated worktree `.worktrees/phase-01-domain-kernel` на branch `phase-01-domain-kernel`. Remote Supabase, Telegram, migrations і production не змінювалися.

## Delivered

- versioned `DungeonContentV1` / domain contracts and JSON Schema draft 2020-12 artifact;
- fully reviewed 10-stage fallback with exact quotas, stage-5 mini-boss and two boss exchanges;
- runtime shape/cross-stage validator, including two check routes plus neutral and rejection of numeric mechanics in content;
- transparent solo/tutorial/partner aggregation;
- deterministic HP/defense/heal/vamp order and caps;
- ordinary stage, trap, mini-boss and boss resolver with explainable breakdown;
- closed V1 registry, canonical JSON, SHA-256 and golden full-run replay;
- Europe/Kyiv 09:00 cycle with two-hour grace and DST tests;
- deterministic early/developed balance simulation.

## Fresh evidence

| Gate | Result |
|---|---|
| `npm run verify` | PASS: format 44 files, lint 29 files, type-check 16 source entrypoints, 57 unit tests and 3 property tests |
| 1000-repeat replay | PASS: byte-identical canonical result and hash on every repeat |
| Golden full run | PASS: 11 resolutions, `victory`, HP 65, XP 150, hash `1d63be460b0517de00bbd2c6ce2bc34e4236992d6d16d7252d2ec210ac20a3b0` |
| Outcome fixtures | PASS: success, neutral, failure, trap, defeated, victory, contained and double-zero prevention |
| Content gates | PASS: exact 10 stages; trap/important/combat/research/social/adjacency/boss/two-check/neutral rules |
| Combat properties | PASS: HP bounds, no resurrection/overheal, actual non-overkill vamp, 8% stage and 25% run caps, boss-death counter suppression |
| Party properties | PASS: exact additive and assist formulas over 10,201 deterministic integer pairs |
| Kyiv cycle | PASS: winter/summer offsets, pre-09:00 ownership, 23-hour spring and 25-hour autumn UTC windows, +2h grace |
| RNG/network scan | PASS: no `Math.random`, `crypto.getRandomValues`, `fetch`, `Deno.connect` or `WebSocket` in domain/scripts |
| Diff integrity | PASS: `git diff --check`; no locked file, migration, DB, Telegram or remote-system change |

## Balance outputs

```json
{"build":"early-teacher","lastCompletedStage":5,"policy":"recommended-checks","remainingHp":0,"replayHash":"3bd58555683a3065621559788672508e58791ff5c8141cb58d98df161dc2b699","terminal":"defeated","xp":43}
{"build":"developed-solo","lastCompletedStage":10,"policy":"recommended-checks","remainingHp":65,"replayHash":"1d63be460b0517de00bbd2c6ce2bc34e4236992d6d16d7252d2ec210ac20a3b0","terminal":"victory","xp":150}
```

## Review fixes

1. Repository `.gitattributes` pins LF after a clean worktree exposed system `core.autocrlf=true`.
2. Companion vitality was not invented: partner contributes approved HP/physical/magical/agility/defense fields; vitality check remains self-owned.
3. Boss neutral/counter/failure percentages have independent versioned config rather than reusing XP percentage.
4. Runtime content rejects unexpected numeric fields and requires at least two check routes plus neutral.
5. Resolver rejects forged nonterminal HP/XP/boss-HP snapshots before resolution.

## Residual constraints

- Thresholds, damage, XP and synthetic builds are prototype numbers pending broader multi-build simulation and later alpha data.
- This fallback validates mechanics; it does not override the stopped concierge sample or prove content variety/retention.
- P01 findings about monotony, combat/stat visibility, early progression and next-day motivation remain required inputs for content/UX phases.
- JSON Schema is retained as a review/integration artifact; the Phase 1 runtime uses the equivalent typed validator without adding a schema-engine dependency.

## Next safe step

Create and review a detailed Phase 2 plan for the persistent atomic core. Do not create migrations, link remote Supabase or deploy until that separate phase gate is explicitly active.
