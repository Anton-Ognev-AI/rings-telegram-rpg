# Phase 0 checkpoint — 2026-07-12

## Verdict

**Status: IN_PROGRESS / human gate pending.**

Усі foundation tasks і Docker-local gate виконані й перевірені. Phase 0 не має статусу `approved`, доки власник не проведе 3–5 concierge sessions та не зафіксує лише анонімні агрегати. Для Phase 1 обрано isolated worktree, але фізично створювати його слід лише після human gate.

## Evidence

| Gate | State | Fresh evidence |
|---|---|---|
| Valid Git baseline | PASS | `main`; commits `45ee953`, `cc76e39`, `af5cb84`, `a558a72`, `d2731a6`, `bd11b22` |
| Reproducible Node install | PASS | `npm ci` exit `0`; 10 packages installed from `package-lock.json` |
| Project-local toolchain | PASS | Node `v24.15.0`; npm `11.12.1`; Deno `2.9.2`; Supabase CLI `2.109.1` |
| Source verification | PASS | `npm run verify`: format 10 files, lint 7 files, type-check 5 source files, unit tests `4 passed / 0 failed` |
| TDD health function | PASS | RED observed on missing `handler.ts`; GREEN `1 passed`; Edge entrypoint type-checks |
| TDD foundation adapters | PASS | RED observed on three missing modules; GREEN clock/Telegram fake/redaction tests `3 passed`; full suite `4 passed` |
| Concierge structure | PASS | validator: 10 stages, 11 player cards, 33 choices, every card `≤700` characters |
| Concierge measurement | PASS | anonymous 18-column template; `first_choice_messages`, `first_choice_taps`, clue/stat comprehension, terminal reason and no participant rows |
| Canonical document integrity | PASS | spec blob `2b5ef89a58b05e6104f84e806f3c9c71bfca5fe3`, size `87476`; master blob `7fb833ed7e04dc57645a700fc3876ccf61514c79`, size `56846`; both equal baseline `45ee953` |
| Repository privacy | PASS | no tracked `.env`; no token-like values; `prototypes/concierge/session-log.csv` is ignored and untracked |
| Supabase local scaffold | PASS | current CLI generated `supabase/config.toml`; idempotent Phase 0 seed exists; no remote project linked |
| Docker local stack + HTTP health | PASS_WITH_CONSTRAINT | Docker Desktop Engine `29.6.1`; `npx supabase start`, `npx supabase db reset` and no-JWT `health` returned `200` with the expected body; stack was stopped after test. Optional Analytics is disabled. On this Windows runtime published ports remained `0.0.0.0`, so local stack is only for a trusted network with synthetic data until a separate network/security decision. |
| Concierge sessions with 3–5 people | PENDING_HUMAN | owner will conduct the sessions; kit is ready and no participant data has been collected |
| Phase 1 workspace isolation | SELECTED_DEFERRED | owner chose `.worktrees/phase-01-domain-kernel`; create it only after the Phase 0 human gate, immediately before Phase 1 work |

## Inline checkpoint fixes

1. Перенесено зі спеки пропущені content quota: stages 1–2 без unconditional trap, ≤3 pure-combat stages, research/social coverage, ≤2 important choices, два boss exchanges.
2. Concierge режим зафіксовано як `post_tutorial_core_loop` із solo snapshot, щоб не змішувати метрики core loop та викладацького onboarding.
3. CSV розширено полями, які реально вимірюють обіцянку першого meaningful choice за два повідомлення/одне натискання.
4. Модератор редагує ту саму Telegram-картку після вибору, а не надсилає HP/XP/outcome окремими повідомленнями.
5. `@std/assert` pinned на `1.0.19`, бо актуальний Deno lint забороняє unversioned JSR imports.
6. Verify scoped на source/toolchain files, щоб не переформатовувати locked `CLAUDE.md` та історичні design/research docs.
7. Supabase CLI у sandbox перевірявся з тимчасовим HOME і вимкненою телеметрією; credentials у repo/user profile не створювалися.
8. `[analytics].enabled = false`: optional Vector/Logflare на Docker Desktop не є вимогою Phase 0 і без окремого secure setup намагався використати Docker log API; core DB/API/Edge Function від цього не залежать.
9. Перевірка custom Docker network не обмежила фактичні host bindings на цій Windows/Docker Desktop конфігурації. Stack зупинено, тестову мережу видалено; Firewall не змінювався.

## Owner decisions recorded

1. Docker Desktop дозволено; local stack/reset/health gate виконано.
2. Для Phase 1 обрано isolated worktree `.worktrees/phase-01-domain-kernel`.
3. Власник сам проведе 3–5 concierge sessions.

## Next safe step

Власник проводить 3–5 sessions за `prototypes/concierge/README.md`, записує лише анонімні агрегати в ignored CSV, після чого Phase 0 повторно класифікується. До `approved` не активувати `PHASE-01` і не створювати worktree.
