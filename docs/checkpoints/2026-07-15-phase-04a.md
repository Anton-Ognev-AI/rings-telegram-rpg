# Phase 4A Final Checkpoint — Local Onboarding and Starter Build

**Date:** 2026-07-16

**Branch:** `codex/phase-04-onboarding-starter-build`

**Status:** `approved` locally. Gates 4.1–4.3 are complete and migration 014 is checksum-pinned and
locked. Phase 4T remains closed: no remote Supabase project, secret, deployment, webhook or real
Telegram identity was used.

## Delivered

- Two actually played tutorial days advance durable progress from `0/2` to `2/2`, with retry-safe
  expiry, abandon and natural-terminal handling and one teacher rescue per eligible tutorial run.
- The player spends earned XP, accepts or discards a no-inventory item, chooses one of four ordinary
  blue rings and sees the resulting server-authoritative build in the third run.
- `player_home_v1`, `start_run_v3`, `request_run_render_v2` and actor/message/profile-bound `pa_`
  actions keep build arithmetic, idempotency and progression mutations on the server.
- Telegram cards remain compact and canonical; pending decisions are restored visibly and one
  stateful run card is edited instead of creating duplicate reward/profile messages.
- The fail-closed 72-cell starter-ring matrix has zero pairwise dominance across baseline, martial
  and arcane test views. The isolated production-baseline diagnostic still records `weapon > fire`,
  `defense > fire` and `healing > fire`.

## Runtime Corrections

The first complete runtime attempt exposed test-fixture drift rather than production defects:

1. two Phase 4 integration fixtures reused Phase 3 synthetic cycle dates and now use unique dates;
2. two E2E files queried obsolete snapshot columns on `game.runs` and now join the canonical
   `run_self_snapshots` and `run_loadout_versions` tables;
3. the defense-ring snapshot fixture now uses armor, so the pinned 15% bonus remains observable
   after integer rounding, while the healing case retains talisman coverage;
4. the natural-terminal fixture now supplies the required `finished_at` timestamp.

No production source, migration, resolver, configuration, fallback content or golden file changed
during these corrections.

## Verification Evidence

```text
focused two-day + starter snapshot E2E       PASS — 3/3
focused tutorial terminal-path E2E           PASS — 1/1
npm run verify:phase4a                        PASS — 31/31 steps, 525.1 s
npm run verify:phase4a (independent repeat)   PASS — 31/31 steps, 553.5 s
npm run verify                               PASS — 168 unit + 3 property tests
npm run simulate:starter                     PASS — 72 reports, 0 full / 3 baseline pairs
migration-checksums.ts --verify              PASS — migrations 001–014
reconciliation / DB lint                     PASS — zero mismatches / zero errors
git diff --check                             PASS
```

Both full runs started from clean local resets, exercised upgrade-from-013, pgTAP, integration,
concurrency, deletion/recovery, Phase 3 Telegram/fault/load regressions and all Phase 4 E2E groups,
then stopped the local Supabase stack through verifier cleanup.

## Docker Recovery

Docker Desktop was installed under `D:\DockerDesktop`, but its engine had stopped responding to
API headers. Only Docker Desktop processes whose executable path was inside that installation were
restarted; no volume, image or local database data was deleted. Docker Desktop 4.81.0 / engine
29.6.1 then completed repeated Supabase start, reset and cleanup cycles. Tests used synthetic local
data only.

## Final Council

The five-role council returned `APPROVE_WITH_SMALL_CHANGES`, all completed by this closeout:

- scope remained limited to Phase 4A tests and canonical documentation;
- server-authoritative architecture and locked Phase 1–3 artifacts stayed unchanged;
- two independent full verifier passes removed the runtime evidence gap;
- migration 014 is now locked and future changes require a new forward migration;
- local approval does not claim production readiness or player-interest validation.

## File Hygiene

Phase-scoped files are `KEEP`; this checkpoint and project memory are `DOC_UPDATE`. No duplicate,
temporary, archive or deletion candidate was found. Tool-managed `.git`, Docker volumes and
`node_modules` were not modified.

## Residual Risks and Next Gate

- Phase 4T owner-smoke is blocked by the real fallback's fire-ring trap until the owner explicitly
  unlocks a focused gameplay-balance amendment or approves production content rotation.
- The 72-cell martial/arcane views are test-only and must not be presented as playable variety.
- Real Telegram UX, secrets, remote migrations, deployment and webhook registration remain a
  separate approval gate.
- The deferred 5–10-person core-loop validation remains unpassed and should follow a successful
  private owner-smoke.

Migration 014 and its checksum are now immutable. The next schema change, if any, must be a new
forward migration; the next content/balance change must not weaken the strict pairwise gate.
